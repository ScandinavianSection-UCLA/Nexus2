import React, {Component} from "react";
import "./GraphView.css";

class GraphView extends Component {
    render() {
        return (
            // contains the entire nexus graph view (the tab)
            <div className="medium-8 cell nexusGraphView">
                {/* Insert graph view code here */}
            </div>
        );
    }
}

export default GraphView;

// react functionality
import React from "react";
// CSS styling
import "./HelpView.css";

class HelpView extends React.Component {
    render() {
        return (
            <div className="HelpView">(insert help content here)</div>
        );
    }
}

export default HelpView;
